<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Page</title>
    <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous"> -->
    <style>
        .mybox {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-left: 10%;
            margin-right: 10%;
            margin-top: 30px;
            border : 2px solid rgb(51, 167, 151);
            padding: 20px;
        }
        h1 {
            text-align: center;
            color: rgb(51, 167, 151);
        }
        pre {
            white-space: pre-wrap; /* Preserve whitespace and wrap as needed */
            word-wrap: break-word; /* Break long words to fit within the container */
            font-family: Arial, sans-serif; /* Use a readable font */
            font-size: 16px; /* Set a comfortable font size */
            line-height: 1.5; /* Improve readability with line spacing */
        }
    </style>
</head>
<body>
    <%- include('partials/header.ejs') %>
    <div class="mybox">
        <!-- <h1>The no of the blog = <%=postno%> </h1> -->
        <h1><%=posts[postno][0]%> </h1>
        <!-- <h2><%=posts[postno][1]%></h2> -->
        <pre> <%= posts[postno][1]%></pre>
        <!-- <button>Edit</button> -->
        <form action="/write" method="POST">
            <button type="submit" name="<%=postno%>">Edit</button>
        </form>
        <form action="/delete" method="POST">
            <button type="submit" name="<%=postno%>" >Delete</button>
        </form>
    </div>
</body>
</html>